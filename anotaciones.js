// Notacion
// ; -> delimitar el final de una instruccion
const a = 1;
a + 2;

// , -> delimitar el inicio de una nueva instruccion

// . -> se utiliza en los objetos para acceder a los atributos
const movil = {
    anchura: 10,
    altura: 20,}
movil.anchura

// [] -> se utitulizan para las listas, arreglos o arrays
const ar = [1,2,3,4,5];
console.log(ar[2]);

// {} -> Se utiliza para los objetos y estructuras de control
const obj = {
    anchura: 10,
    altura: 20
}

if (true) {
    const constante2 = "hola";
}

// () -> Se utiliza para las funciones
function sumar(a, b) {
    return a + b
}
